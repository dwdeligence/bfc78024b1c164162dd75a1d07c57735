services:
  mysql:
    image: 'mariadb:lts'
    ports:
      - '${WP_ENV_MYSQL_PORT:-}:3306'
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: wordpress
    volumes:
      - 'mysql:/var/lib/mysql'
  tests-mysql:
    image: 'mariadb:lts'
    ports:
      - '${WP_ENV_TESTS_MYSQL_PORT:-}:3306'
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: tests-wordpress
    volumes:
      - 'mysql-test:/var/lib/mysql'
  wordpress:
    depends_on:
      - mysql
    build:
      context: .
      dockerfile: WordPress.Dockerfile
      args: &ref_0
        HOST_USERNAME: miguelm
        HOST_UID: '1000'
        HOST_GID: '1000'
    ports:
      - '${WP_ENV_PORT:-8888}:80'
    environment:
      APACHE_RUN_USER: '#1000'
      APACHE_RUN_GROUP: '#1000'
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: wordpress
      WP_TESTS_DIR: /wordpress-phpunit
    volumes: &ref_1
      - >-
        /home/miguelm/wp-env/bfc78024b1c164162dd75a1d07c57735/WordPress:/var/www/html
      - >-
        /home/miguelm/wp-env/bfc78024b1c164162dd75a1d07c57735/WordPress-PHPUnit/tests/phpunit:/wordpress-phpunit
      - 'user-home:/home/miguelm'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
  tests-wordpress:
    depends_on:
      - tests-mysql
    build:
      context: .
      dockerfile: Tests-WordPress.Dockerfile
      args: *ref_0
    ports:
      - '${WP_ENV_TESTS_PORT:-8889}:80'
    environment:
      APACHE_RUN_USER: '#1000'
      APACHE_RUN_GROUP: '#1000'
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: tests-wordpress
      WORDPRESS_DB_HOST: tests-mysql
      WP_TESTS_DIR: /wordpress-phpunit
    volumes: &ref_2
      - >-
        /home/miguelm/wp-env/bfc78024b1c164162dd75a1d07c57735/tests-WordPress:/var/www/html
      - >-
        /home/miguelm/wp-env/bfc78024b1c164162dd75a1d07c57735/tests-WordPress-PHPUnit/tests/phpunit:/wordpress-phpunit
      - 'tests-user-home:/home/miguelm'
    extra_hosts:
      - 'host.docker.internal:host-gateway'
  cli:
    depends_on:
      - wordpress
    build:
      context: .
      dockerfile: CLI.Dockerfile
      args: *ref_0
    volumes: *ref_1
    user: '1000:1000'
    environment:
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: wordpress
      WP_TESTS_DIR: /wordpress-phpunit
    extra_hosts:
      - 'host.docker.internal:host-gateway'
  tests-cli:
    depends_on:
      - tests-wordpress
    build:
      context: .
      dockerfile: Tests-CLI.Dockerfile
      args: *ref_0
    volumes: *ref_2
    user: '1000:1000'
    environment:
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: tests-wordpress
      WORDPRESS_DB_HOST: tests-mysql
      WP_TESTS_DIR: /wordpress-phpunit
    extra_hosts:
      - 'host.docker.internal:host-gateway'
  phpmyadmin:
    image: phpmyadmin
    ports:
      - '${WP_ENV_PHPMYADMIN_PORT:-}:80'
    environment:
      PMA_PORT: 3306
      PMA_HOST: mysql
      PMA_USER: root
      PMA_PASSWORD: password
  tests-phpmyadmin:
    image: phpmyadmin
    ports:
      - '${WP_ENV_TESTS_PHPMYADMIN_PORT:-}:80'
    environment:
      PMA_PORT: 3306
      PMA_HOST: tests-mysql
      PMA_USER: root
      PMA_PASSWORD: password
volumes:
  mysql: {}
  mysql-test: {}
  user-home: {}
  tests-user-home: {}
